SQLite Bible Database

This repository hosts a pre-populated SQLite database used as a downloadable data source for client applications.

The database is distributed via GitHub Releases to ensure a stable, direct download link.
